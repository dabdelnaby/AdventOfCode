# First part of ADVENT OF CODE for DAY 4

import argparse

parser = argparse.ArgumentParser(description='Finds games that are possible')
parser.add_argument('file', help='Input the file')
arg = parser.parse_args()